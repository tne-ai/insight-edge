import pandas as pd
from tne.TNE import TNE

# Initialize session and get data
session = TNE(uid=UID, bucket_name=BUCKET, project=PROJECT, version=VERSION)
df = session.get_object("womens_rtw-shopping_list-442-1_1-with_periods-with_sizes_cleaned.csv")

# Filter data for 'WOMENS PANTS' in the 'product_subgroup_desc' column
womens_pants_data = df[df['product_subgroup_desc'] == 'WOMENS PANTS']

# Group by 'period' and sum the 'sales' column to find the month with the highest sales
sales_by_month = womens_pants_data.groupby('period')['sales'].sum()

# Find the month with the highest sales
best_month = sales_by_month.idxmax()

# Store the result
result = best_month