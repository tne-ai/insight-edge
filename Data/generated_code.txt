import pandas as pd
from tne.TNE import TNE

# Initialize session and get data
session = TNE(uid=UID, bucket_name=BUCKET, project=PROJECT, version=VERSION)
df = session.get_object("womens_rtw-shopping_list-442-1_1-with_periods-with_sizes_cleaned.csv")

# Filter data for Women's Pants and size 14
filtered_df = df[
    (df['product_group_description'] == 'WOMENS PANTS') &
    (df['size_code'] == 14)
]

# Group by colour_description and sum sales
sales_by_colour = filtered_df.groupby('colour_description')['sales'].sum().reset_index()

# Sort by sales in descending order to find the best-selling colour
sales_by_colour = sales_by_colour.sort_values(by='sales', ascending=False)

# Limit to the top result
result = sales_by_colour.head(1)