from tne.TNE import TNE
import pandas as pd

session = TNE(uid=UID, bucket_name=BUCKET, project=PROJECT, version=VERSION)
df = session.get_object("handbags-shopping_list-no_run-1_1-with_periods_cleaned.csv")

# Group by color and calculate the average AUR
color_aur_avg = df.groupby('colour_description')['aur'].mean().reset_index()

# Sort the colors by average AUR in descending order
color_aur_avg_sorted = color_aur_avg.sort_values(by='aur', ascending=False).head(12)

# Rename columns for clarity
color_aur_avg_sorted.columns = ['Color', 'Average AUR']

result = color_aur_avg_sorted