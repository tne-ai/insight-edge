import pandas as pd
from tne.TNE import TNE

# Initialize session and get data
session = TNE(uid=UID, bucket_name=BUCKET, project=PROJECT, version=VERSION)
df = session.get_object("womens_rtw-shopping_list-442-1_1-with_periods-with_sizes_cleaned.csv")

# Filter for Women Shorts
df_shorts = df[df['product_subgroup_desc'] == 'WOMENS SHORTS']

# Group by style_color_description and sum sales
top_styles = df_shorts.groupby('style_color_description')['sales'].sum().reset_index()

# Sort by sales in descending order and get top 10
top_styles = top_styles.sort_values(by='sales', ascending=False).head(10)

# Rename columns for clarity
top_styles.columns = ['Style Color Description', 'Total Units Sold']

# Store the result
result = top_styles