import pandas as pd
from tne.TNE import TNE

# Initialize session and get data
session = TNE(uid=UID, bucket_name=BUCKET, project=PROJECT, version=VERSION)
df = session.get_object("womens_rtw-shopping_list-442-1_1-with_periods-with_sizes_cleaned.csv")

# Filter the dataframe for Women Skirts, Newness, and Tail
filtered_df = df[(df['product_subgroup_desc'] == 'WOMENS SKIRTS') & 
                 (df['range_segment'] == 'Newness') & 
                 (df['pareto'] == 'Tail')]

# Select the relevant columns and sort by size_code
result = filtered_df[['size_code', 'style_color_description', 'sales', 'end_inv', 'total_stock']].sort_values(by='size_code').head(12)