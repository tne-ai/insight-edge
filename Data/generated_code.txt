import pandas as pd
from tne.TNE import TNE

# Initialize session and get data
session = TNE(uid=UID, bucket_name=BUCKET, project=PROJECT, version=VERSION)
df = session.get_object("handbags-shopping_list-no_run-1_1-with_periods_cleaned.csv")

# Filter data for the 'Newness' range_segment
newness_data = df[df['range_segment'] == 'Newness']

# Group by period and calculate total sales for each month
monthly_sales = newness_data.groupby('period')['sales'].sum().reset_index()

# Sort by period
monthly_sales = monthly_sales.sort_values(by='period')

# Rename columns for clarity
monthly_sales.columns = ['Period', 'Total Sales']

# Limit to 12 rows
result = monthly_sales.head(12)